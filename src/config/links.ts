// Link da Amazon. Hoje `null` → CTAs mostram "Adquirir na Amazon — em breve" (desabilitado).
// Quando o livro for publicado, troque por uma URL string → CTAs viram link ativo (num lugar só).
export const AMAZON_URL: string | null = null;
